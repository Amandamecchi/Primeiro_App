import "../styles/globals.css";

			export const metadata = {
				title: "Bear - Bad Time",
			};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
		}